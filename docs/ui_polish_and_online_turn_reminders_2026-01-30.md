# UI 美化与联机行动提醒改进（进度追踪）

日期：2026-01-30

目标：
- 优化游戏内「菜单 / 弹窗对话框」视觉设计与一致性（更接近成熟产品：清晰层级、统一间距、主次按钮区分、对比度与可读性）。
- 补齐联机对局中的关键交互提醒（例如：轮到当前玩家行动 / 轮到你行动）。
- 顺带排查并修复其它明显的交互型 UI 问题（不改变规则与核心逻辑）。

约束：
- 每个功能点使用独立 git commit。
- 每次更新（每个 commit）后必须跑：`ui/scenes/tests/game_smoke_test.tscn` 与 `ui/scenes/tests/all_tests.tscn`。
- 若遇到需要澄清的问题：记录在本文档「待澄清问题」中并先跳过，优先完成无歧义部分。

## 进度清单

### 0) 追踪与基线
- [x] 建立本进度文档并记录当前问题点（本条仅用于初始化）

### 1) 弹窗 / 对话框（视觉与一致性）
- [x] `ui/dialogs/confirm_dialog.tscn`：统一背景、圆角、阴影、主次按钮区分
- [x] `ui/dialogs/choice_dialog.tscn`：同上，并优化选项区域可读性与间距
- [x] `ui/dialogs/settings_dialog.tscn`：同上，并统一底部操作区按钮样式
- [x] `ui/dialogs/save_load_dialog.gd`：增加内边距与背景容器，减少“顶边贴边”的廉价感
- [x] `ui/scenes/game/game.tscn`（`MenuDialog`）：菜单弹窗同风格

### 2) 菜单页（主菜单 / 设置前置 / 联机大厅）
- [x] `ui/scenes/main_menu.tscn`：增加视觉层级（标题、按钮组、版本信息），统一按钮尺寸与间距
- [x] `ui/scenes/setup/game_setup.tscn`：提升信息密度与可读性（分组、边距、按钮主次）
- [x] `ui/scenes/online/online_lobby.tscn`：统一标题栏与内容区层级，弱化表单“堆砌感”

### 3) 联机对局关键提醒
- [ ] In-game 提示：仅在“轮到你行动”时提醒（不提示轮到其他玩家）
- [ ] In-game 音效：轮到你行动播放提示音（当前资源留空，先走 SoundManager 占位）
- [ ] In-game 阶段切换：所有“大阶段”切换都需要提示（所有模式都显示；Working 内子阶段不提示；暂不做音效）

### 4) 其它交互问题扫尾
- [x] 联机大厅：加入加密房间的密码输入弹窗改为统一样式（避免默认 `ConfirmationDialog` 观感割裂）
- [x] 员工升级路线：员工详情弹窗改为统一样式（避免默认 `AcceptDialog` 观感割裂）
- [x] 排查：按钮不可用时缺少解释/提示（例如联机未连接仍可点）（联机大厅已补齐未连接/未入房时的状态提示）
- [x] 排查：窗口 close / cancel 行为不一致（ESC、关闭按钮）（主要弹窗已支持 ESC 关闭）
- [x] 排查：重要弹窗缺少默认焦点与键盘操作（主要弹窗已设置默认焦点/回车提交）

### 5) 弹窗体系升级（Window → Control 模态）
- [ ] 将确认/选择/设置/存档/密码/信息弹窗改为 `Control` 模态（更易定制、统一样式、可动画）
- [ ] 清理旧 `Window` 弹窗依赖（避免双体系共存造成维护成本）

### 6) 联机大厅重做（去 Tab、多页面导航）
- [ ] 页面拆分为独立组件：连接页 / 房间列表页 / 创建房间页 / 房间内页（不使用 Tab）
- [ ] “创建房间”改为房间列表页按钮 → 进入创建页；创建后自动进入房间内页；可返回房间列表
- [ ] 联机昵称同步：局内玩家名与联机连接填写一致（修复）

### 7) 模块选择 UI 强化
- [ ] 模块选择列表：每组模块增加不同颜色背景，明确分组归属（本地设置/联机创建/房间内配置共用）

## 已观察到的初始问题点（待处理）
- 多数 Window 弹窗视觉较“裸”，缺少统一的背景、圆角、阴影与内边距层级。
- 主菜单/设置页/联机大厅布局偏“原型”，缺少成熟产品常见的分组与视觉层级（Panel、标题区、辅助说明）。
- 联机对局中虽然存在 `CurrentPlayerLabel` 文本，但缺少“轮到你行动”的显著提醒（易错过）。

## 待澄清问题（先记录，后统一询问）
1) 阶段切换提示的文案偏好：仅显示“进入阶段：X”，还是需要同时显示回合数/当前玩家？
2) 联机大厅“房间内页”是否需要显示房主/准备状态/人数条件等更清晰的信息层级？（目前先按现有字段渲染）

## 回归测试
- `tools/run_headless_test.sh res://ui/scenes/tests/game_smoke_test.tscn GameSmokeTest 60`
- `tools/run_headless_test.sh res://ui/scenes/tests/all_tests.tscn AllTests 60`
