# Reserve Prices（模块14）

## 规则要点

- 游戏开始时使用“替代储备卡”集合（每位玩家 3 张）。
- 银行第一次破产时：
  - 银行固定增加 `$200 × 玩家人数`（不再按储备卡决定注资金额）。
  - 储备卡不再影响 CEO 卡槽数。
  - 统计所有玩家“已选择的储备卡”的 `type`（5/10/20），用出现最多的类型作为新的 `base_unit_price`，后续晚餐均使用该基础单价。
  - 若出现次数并列，按优先级 `20 > 5 > 10` 决胜。

